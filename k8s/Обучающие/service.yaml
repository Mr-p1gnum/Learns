apiVersion: v1
kind: Service
metadata:
  name: my-backend-service
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: NodePort  # может быть ClusterIP, NodePort